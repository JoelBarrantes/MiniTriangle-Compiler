! Ra�z entera de un n�mero no negativo
! Se basa en la suma de los primeros N n�meros naturales impares.
! Versi�n anotada.

let
	var a : Char;       ! dato de entrada
	var r : Integer;       ! resultado = ra�z cuadrada entera (piso de ra�z)
	const term ~ a;   ! siguiente impar por considerar
	!proc testt(var x: Integer) ~ testt(var x) end;
	recursive
		func pot4(num: Integer): Integer ~ num * pot3(num)
		and
		func pot2(num: Integer): Integer ~ num*num
		and
		func pot3(num: Integer): Integer ~ num * pot2(num)
		and
		proc test(var num: Integer) ~ a := 'T'; test(var num) end
		and
		func pot5(num: Integer): Integer ~ num * pot4(num)
		and 
		proc test1(a: Char) ~ test2(a) end
		and
		proc test2(b: Char) ~ test1(b) end


	end;
	var init := 5
	
in 
	a := 'P';
	r := 3;
	test(var r);
	r := 2
end

